{
  "project_name": "openclaw-sandbox-lab",
  "backup_type": "source_snapshot_without_paperclip",
  "source_path": "/home/user/workspace/openclaw-sandbox-lab",
  "excluded_paths": [
    "paperclip*",
    ".git",
    "node_modules",
    "dist",
    "build",
    ".next",
    "coverage",
    "__pycache__",
    ".venv",
    "venv"
  ],
  "required_runtime": {
    "node": "20+",
    "npm": "10+"
  },
  "app": {
    "dev_command": "npm install && npm run dev",
    "build_command": "npm run build",
    "start_command": "npm run start",
    "default_port": "5000"
  },
  "database": {
    "engine": "postgresql",
    "orm": "drizzle",
    "required_env": [
      "DATABASE_URL"
    ],
    "warning": "Do not run npm run db:push against production without explicit approval."
  },
  "restore_sequence": [
    "Clone repository",
    "Copy .env.example to .env and fill required values",
    "Run npm install",
    "Run npm run dev for local recovery or npm run build && npm run start for production-like run",
    "Only run database migration commands after validating the target database is safe"
  ]
}